#include <cstdlib>
#include <iostream>

using namespace std;

public int main(char[] *args, int argNum) { // I can't remember if this is the correct number of arguments for a "main" function
  system("PAUSE");
  exit(0);
}
